name: 'Dependency Updates'

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main

jobs:
  call-e2e-merge:
    uses: khulnasoft/actions/.github/workflows/e2e-merge.yml@main
    with:
      branch: 'main'
    secrets:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
